version: '3'

services:

  consul-server-3:
    image: 'hashistack-alpine:1.0.0'
    container_name: 'consul-server-3'
    restart: 'unless-stopped'
    labels:
    - 'traefik.enable=true'
    - 'traefik.http.routers.consul.rule=Host(`consul.docker.localhost`)'
    - 'traefik.http.services.consul.loadbalancer.server.port=8500'
    - 'traefik.http.routers.consul.tls=true'
    volumes:
    - './configs/server.hcl:/home/root/consul.hcl:ro'
    - '../certs:/home/root/certs:ro'
    networks:
    - 'public-subnet'
    command: ['/root/bin/consul', 'agent', '-encrypt=${CONSUL_ENCRYPT}', '-config-file=/home/root/consul.hcl', '-node=node-consul-server-3']

networks:

  public-subnet:
    external: true
